# Configuration Directory

This folder stores runtime configuration artifacts such as the cached Roon
pairing token (`roon-token.json`). The file is created automatically the first
time the controller successfully pairs with a Roon Core.

> **Note:** The token file is excluded from version control via `.gitignore`.
